import { connect } from 'react-redux';

import EditBatch from './edit_batch';

const mapStateToProps = dispatch => ({
	
});

const mapDispatchToProps = dispatch => ({
	
});

export default connect(
	mapStateToProps,
	mapDispatchToProps
)(EditBatch);